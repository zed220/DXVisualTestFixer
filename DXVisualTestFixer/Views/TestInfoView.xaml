<UserControl x:Class="DXVisualTestFixer.Views.TestInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DXVisualTestFixer.Views"
             xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
             xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:behaviors="clr-namespace:DXVisualTestFixer.Behaviors"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <dxdo:LayoutGroup Orientation="Vertical" ShowCaption="False">
        <dxdo:LayoutGroup DataContext="{Binding TestInfo}">
            <dxdo:LayoutGroup.Resources>
                <Style TargetType="{x:Type dxdo:LayoutPanel}">
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <ScrollViewer HorizontalScrollBarVisibility="Auto">
                                    <Image Source="{TemplateBinding Content}" Stretch="Uniform"/>
                                </ScrollViewer>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </dxdo:LayoutGroup.Resources>
            <dxdo:LayoutPanel ShowCaption="True" Caption="Before" ShowCloseButton="False" ShowPinButton="False" Content="{Binding ImageBefore}"/>
            <dxdo:LayoutPanel ShowCaption="True" Caption="Current" ShowCloseButton="False" ShowPinButton="False"  Content="{Binding ImageCurrent}"/>
            <dxdo:LayoutPanel ShowCaption="True" Caption="Diff" ShowCloseButton="False" ShowPinButton="False" Content="{Binding ImageDiff}"/>
        </dxdo:LayoutGroup>
        <dxdo:LayoutPanel ItemHeight="150" ShowCaption="False" DataContext="{Binding TestInfo}">
            <avalonedit:TextEditor FontFamily="Consolas" IsReadOnly="True">
                <dxmvvm:Interaction.Behaviors>
                    <behaviors:AvalonEditTextBindingBehavior Text="{Binding Path=TextDiff}"/>
                </dxmvvm:Interaction.Behaviors>
            </avalonedit:TextEditor>
        </dxdo:LayoutPanel>
        <dxdo:LayoutPanel ItemHeight="Auto" ShowBorder="False" ShowCaption="False">
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Prev"/>
                <Button Content="Next"/>
                <Button Content="Ok"/>
                <Button Content="Skip"/>
            </StackPanel>
        </dxdo:LayoutPanel>
    </dxdo:LayoutGroup>
</UserControl>
